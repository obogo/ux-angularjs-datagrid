describe("columns", function () {
    it("should get the names of all columns in the data-width attributes of the children to the row", function () {

    });

    it("should get all of the data in the width attributes of the children to the row.", function () {

    });

    it("should convert percent values to fixed values", function () {

    });

    it("should update on a resize", function () {

    });

    it("should only update if there is a width", function () {

    });

    it("should spread percents over the width that is remaining after all fixed widths have been accounted for", function() {

    });
});